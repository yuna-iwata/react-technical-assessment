import React from "https://dev.jspm.io/react@17.0";

/**
 * @todo ================TASK================
 * Create a Header component named 'Header'
 *
 * 1. Use a <header> tag and set the class to be `App-header`
 * 2. Add a <h1> tag inside the <header> with the title "Code Quotes"
 * */

export default function Header() {
  return (
    <header className="App-header">
      <h1>Code Quotes</h1>
    </header>
  );
}
